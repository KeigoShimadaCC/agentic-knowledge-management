/**
 * Re-exports sonner's Toaster and toast helper.
 * @example In layout.tsx: <Toaster />
 * @example In component: toast.success("Saved"), toast.error("Failed", { description: err.message })
 */
export { Toaster } from "sonner";
export { toast } from "sonner";
