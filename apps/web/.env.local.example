# Copy to `.env.local` when running the web app on your machine with `pnpm dev`
# while the API runs in Docker (published on host port 8001):
#
#   cp .env.local.example .env.local
#
API_URL=http://127.0.0.1:8001
NEXT_PUBLIC_API_URL=http://127.0.0.1:8001
